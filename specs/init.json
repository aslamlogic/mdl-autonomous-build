{
  "system": {
    "name": "meta-dev-api",
    "type": "fastapi",
    "description": "API with GET and POST endpoints"
  },

  "api": {
    "basePath": "/",
    "endpoints": [
      {
        "name": "root",
        "method": "GET",
        "path": "/",
        "description": "Root endpoint",

        "response": {
          "type": "object",
          "schema": {
            "message": {
              "type": "string",
              "example": "Welcome to the meta-dev API!"
            }
          }
        }
      },

      {
        "name": "health",
        "method": "GET",
        "path": "/health",
        "description": "Health check",

        "response": {
          "type": "object",
          "schema": {
            "status": {
              "type": "string",
              "example": "healthy"
            }
          }
        }
      },

      {
        "name": "echo",
        "method": "POST",
        "path": "/echo",
        "description": "Echo input data",

        "request": {
          "type": "object",
          "schema": {
            "text": {
              "type": "string",
              "example": "hello"
            }
          }
        },

        "response": {
          "type": "object",
          "schema": {
            "echo": {
              "type": "string",
              "example": "hello"
            }
          }
        }
      }
    ]
  }
}
